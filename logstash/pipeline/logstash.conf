input {
  kafka {
    bootstrap_servers => "kafka:9092"
    topics => ["app-logs"]
    codec => json
  }
}

filter {
  grok {
    match => {
      "message" => "%{LOGLEVEL:level} %{TIMESTAMP_ISO8601:timestamp} %{GREEDYDATA:msg}"
    }
  }

  date {
    match => ["timestamp", "ISO8601"]
    target => "@timestamp"
  }

  # Optional structured fields embedded in the message body, e.g.
  # "User login successful user_id=42 latency_ms=87". Not every message
  # carries these, so a non-match just tags (rather than fails) the event.
  grok {
    match => { "msg" => "user_id=%{NUMBER:user_id:int}" }
    tag_on_failure => ["_no_user_id"]
  }

  grok {
    match => { "msg" => "latency_ms=%{NUMBER:latency_ms:int}" }
    tag_on_failure => ["_no_latency"]
  }

  mutate {
    remove_field => ["timestamp"]
    remove_tag => ["_no_user_id", "_no_latency"]
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
